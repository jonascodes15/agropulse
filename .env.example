# Copy to .env at the repo root and adjust if needed.
# Only used once you wire the frontend up to the real API (see
# src-additions/hooks/useLiveReadings.ts) — the frontend works fine without
# this file using its existing mock data.
VITE_API_URL=http://localhost:8000
